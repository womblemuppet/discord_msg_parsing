Small fun project to create word bigrams and trigrams (n-grams) based on a dump of discord server messages.

The resulting output was used for a discord bot to (rather primitively) generate sentences in the style of a particular user.

